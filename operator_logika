#include <iostream>
using namespace std;

int main() {
    cout << "=== Operator Logika ===\n";
    cout << "true && false -> " << (true && false) << endl;
    cout << "true || false -> " << (true || false) << endl;
    cout << "!true -> " << (!true) << endl;

    return 0;
}
